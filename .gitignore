# ── Dependencies & build output ───────────────────────────
node_modules/
.next/

# ── Editor / tooling / local state ────────────────────────
.claude/
.playwright-mcp/
.theme-backups/
.DS_Store
*.log

# ── Loose scratch screenshots at workspace root ───────────
/*.png

# ── Separate git repos (each has its own .git + own remote;
#    not part of this monorepo) ─────────────────────────────
princetires-app/
superpowers/
taste-skill/
ui-ux-pro-max-skill/
impeccable/
awesome-claude-skills/
awesome-design-md/

# ── Secrets ───────────────────────────────────────────────
.env
.env.local
*.local-bak

# Git worktrees
.worktrees/
